export enum AddressType {
  Billing = 'billing',
  Shipping = 'shipping',
}

export enum ProductType {
  Simple = 'simple',
  Variable = 'variable',
  Bundle = 'bundle',
}

export enum Role {
  Admin = 'admin',
  User = 'user',
}